name: build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run:
    name: build
    timeout-minutes: 15
    strategy:
      fail-fast: true
      matrix:
        container:
          - ghcr.io/c-cube/c-cube-commmon/ci-4.14:latest
          - ghcr.io/c-cube/c-cube-commmon/ci-doc-5.3:latest

    runs-on: ubuntu-latest
    container: ${{ matrix.container }}
    steps:
      - uses: actions/checkout@v6
      - run: opam pin -n .
      - run: opam depext -yt ocaml-lua ezlua ppx_ezlua
      - run: opam install -t . --deps-only
      - run: opam exec -- dune build @install
      - run: opam exec -- dune runtest
